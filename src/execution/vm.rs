
use super::data::*;

/*
struct RetAddr {
    func: usize,
    loc: usize,
}

struct Frame { 
    local : Vec<VmData>,
    ret : Option<RetAddr>
}

pub struct Vm {
    memory : Vec<VmData>,
    stack : Vec<Frame>,
    ret : Option<VmData>,
}

impl Vm {
    pub fn new() -> Self {
        Vm { memory : vec![], stack : vec![], ret : None }
    }

    pub fn run(&mut self, main : usize, functions : &[Vec<Instr>]) {
        
    }
}
*/